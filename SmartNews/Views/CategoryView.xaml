<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="SmartNews.Views.CategoryView">
    <yummy:PancakeView
            Margin="10"
            BackgroundGradientAngle="5"
            BackgroundGradientEndColor="Azure"
            BackgroundGradientStartColor="LightSkyBlue"
            CornerRadius="5"
            HasShadow="True"
            HeightRequest="100">
        <StackLayout>
            <StackLayout.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnCategoryTapped" />
            </StackLayout.GestureRecognizers>
            <Image Source="{Binding UrlImages}" HorizontalOptions="Center" Margin="0,10,0,0"/>
            <Label Text="0" TextColor="Red" HorizontalOptions="EndAndExpand" Margin="0,-20,10,0"/>
            <Label
                Margin="0,0,0,10"
                FontSize="18"
                Text="{Binding TitleBar}"
                HorizontalOptions="Center" />
        </StackLayout>
    </yummy:PancakeView>
</StackLayout>
