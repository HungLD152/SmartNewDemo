<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="SmartNews.Views.CategoryView">
    <yummy:PancakeView
            Margin="10"
            BackgroundGradientAngle="5"
            BackgroundGradientEndColor="Azure"
            BackgroundGradientStartColor="LightSkyBlue"
            CornerRadius="5"
            HeightRequest="100"            >
        <StackLayout>
        <Image Source="icon-setting.png" HorizontalOptions="Center" Margin="0,10,0,0"/>
            <Label Text="12" TextColor="Red" HorizontalOptions="EndAndExpand" BackgroundColor="LightGreen" Margin="0,-20,10,0"/>
            <Label
                Margin="0,0,0,10"
                FontSize="18"
                Text="{Binding TitleBar}"
                HorizontalOptions="Center" />
            </StackLayout>
        </yummy:PancakeView>
</StackLayout>
